-- Comment.nvim was removed: Neovim 0.10+ has native `gc`/`gcc` commenting that uses
-- `vim.bo.commentstring` set by the built-in ftplugins (cs.lua, lua.lua, etc.).
-- The obsession statusline tweak that lived in the old init = function() now lives in obsession.lua.
return {}
